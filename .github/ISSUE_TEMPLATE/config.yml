# Force every report through a template — a blank issue from a tester is
# almost always missing the version, the device, or the screenshot, and
# recovering that costs a round-trip.
blank_issues_enabled: false
